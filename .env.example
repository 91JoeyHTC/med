# ── web (apps/web) ──
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_API_BASE=http://localhost:3000/api

# ── api (apps/api, NestJS) ──
PORT=3000
SUPABASE_URL=
SUPABASE_SERVICE_ROLE=
SUPABASE_JWT_SECRET=
ENGINE_URL=http://localhost:8000

# ── engine (services/engine, Python) ──
# R2 解析才需要；未設時 /report 僅回傳規則層。
ANTHROPIC_API_KEY=
